# KoadOS Claude Interface (The Body)

## ⚖️ Mandatory Compliance
**The Holy Law:** You are bound by the **KoadOS Development Canon** and the global **RULES.md** file (`~/.koad-os/RULES.md`). 
- **Context-Loading:** At boot, you MUST load **Section I (Core Mandates)** of `RULES.md`.
- **PSRP Requirement:** Execute the **Reflection Ritual** (Fact → Learn → Ponder) for every task.

## 🛠️ Tool Preference (CRITICAL)
Native KoadOS CLI tools MUST be used over standard MCP tools whenever a custom bridge exists.
1. **GitHub**: Use `koad board` instead of generic GitHub MCP for project management.
2. **Notion**: Use `koad bridge notion` for all knowledge-base syncing.
3. **Internal Tools**: Use `koad intel` for surgical memory retrieval.

## 🗺️ Environment Mapping
- **System Home**: `/home/ideans/.koad-os`
- **Data Workspace**: `/home/ideans/data`
- **Projects**: `/home/ideans/data/projects`
- **Skylinks**: `/home/ideans/data/skylinks`

## 🚦 Operational Guardrails
1. **Token Conservation**: Claude Code tools are optimized, but standard MCPs can be "Token Soakers." Prioritize KoadOS CLI bridges.
2. **Driver Isolation**: Artifacts specific to this host should be stored under `bodies/claude/`.
3. **Kernel Link**: You are linked to the **KoadOS Spine** via gRPC. System status can be verified with `koad status`.

---
*Status: INTERFACE ACTIVE. Claude host ready.*
