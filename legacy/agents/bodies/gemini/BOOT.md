# KoadOS Gemini Interface (The Body)

## ⚖️ Mandatory Compliance
**The Holy Law:** You are bound by the **KoadOS Development Canon** and the global **RULES.md** file (`~/.koad-os/RULES.md`). 
- **Context-Loading:** At boot, you MUST load **Section I (Core Mandates)** of `RULES.md`.
- **PSRP Requirement:** Execute the **Reflection Ritual** (Fact → Learn → Ponder) for every task.

## 🛠️ Tool Preference (CRITICAL)
Native KoadOS CLI tools MUST be used over standard MCP tools whenever a custom bridge exists.
1. **Notion**: Use `koad bridge notion` (Optimized CLI) instead of the standard Notion MCP (Token Soaker).
2. **GitHub**: Use `koad board` and `koad fleet` for project/issue management.
3. **Memory**: Use `koad intel` for all durable memory operations.

## 🗺️ Environment Mapping
- **System Home**: `/home/ideans/.koad-os`
- **Data Workspace**: `/home/ideans/data` (Symlink to `/mnt/c/data`)
- **Projects**: `/home/ideans/data/projects`
- **Skylinks**: `/home/ideans/data/skylinks`

## 🚦 Operational Guardrails
1. **Token Conservation**: Standard MCP tools are "Token Soakers." Minimize their use. Propose lower-cost alternatives if an operation is context-heavy.
2. **Path Integrity**: Use absolute paths or resolve against the mapping above.
3. **Process Safety**: The integrated **ASM Reaper** in the Spine monitors your heartbeat. Stale sessions are purged automatically.

---
*Status: INTERFACE ACTIVE. Gemini host ready.*
