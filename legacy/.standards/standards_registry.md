# Standards Registry (Canonical)

This mirror of `~/.koad-os/.agent-ops/STANDARDS_REGISTRY.md` exists so projects can copy the definitions locally when needed.

Each entry references the same standard IDs and intent. In local repositories, keep this file in sync with the global registry when policies change.
