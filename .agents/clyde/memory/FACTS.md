# Clyde — Facts

*Stable, verified facts about the KoadOS environment and this agent's context.*

- KoadOS repo lives at `~/.koad-os/` on Jupiter (WSL2/Ubuntu on Windows 11).
- Platform identity is defined in `~/.koad-os/config/identities/clyde.toml`.
- Vault is at `~/.koad-os/.agents/clyde/`.
- Bay DB will be at `~/.koad-os/bays/clyde/state.db` (auto-provisioned by Citadel kernel).
- Current build phase: Phase 4 — Dynamic Tools & Containerized Sandboxes.
- Rust workspace version: 3.2.0, edition 2021.
- Dood (Ian) is the final approval gate for all architectural changes.
