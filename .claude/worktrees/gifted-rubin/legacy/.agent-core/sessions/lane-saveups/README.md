# Lane Saveups

Store lane-isolated continuity records here, one file per lane context (e.g., `lane/Platform/BL-012.md`).

Lane agents should never commit these files to release branches. They exist purely to capture intermediate lessons until the work reaches the `koad-os` support branch for reconciliation.
