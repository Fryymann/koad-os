# Task: Phase 4 Handoff — Dynamic Plugin System & Sandboxing

## 🧩 Context
We have initiated **Phase 4: Dynamic Tools & Containerized Sandboxes**. The architectural decisions are finalized, and initial scaffolding is complete. However, the implementation of the WASM plugin host is currently blocked by compilation errors.

## 🎯 Current Objective
Stabilize the `koad-plugins` crate and verify the end-to-end WASM plugin execution flow.

## 🚧 The Blocker: bindgen! Name Resolution
The `koad-plugins` crate uses `wasmtime::component::bindgen!` to generate host-side bindings from `wit/hooks.wit`. 
- **The Issue:** I have attempted multiple variations to reference the generated modules/traits (e.g., `koad::hooks::citadel_hooks::Host`, `hooks::citadel_hooks::Host`, etc.), but `rustc` consistently reports "unresolved module or unlinked crate."
- **Current Error Baseline:**
  ```text
  error[E0433]: failed to resolve: use of unresolved module or unlinked crate `koad`
  --> crates/koad-plugins/src/lib.rs:18:6
  |
  18 | impl koad::hooks::citadel_hooks::Host for MyHostState {
  ```

## 📋 Instructions for Claude
1.  **Fix the Bindings:** Analyze `crates/koad-plugins/src/lib.rs` and the `bindgen!` macro output. Identify the correct module path generated by `wasmtime` 22.0 and update the `Host` trait implementation and the `add_to_linker` call.
2.  **Verify Host Stability:** Run `cargo check -p koad-plugins` until clean.
3.  **Build the Guest:** Compile `crates/koad-plugins/examples/hello-plugin` to `wasm32-wasip1`. (Note: Tyr already added the target to the toolchain).
4.  **Run Integration Test:** Execute the test in `src/lib.rs` (`test_plugin_manager_runs_wasm`) to confirm the host can load and run the guest.
5.  **Next Steps:** Once the plugin system is stable, proceed to the next Phase 4 milestones:
    - **MCP Tool Registry:** Implement tool discovery in CASS.
    - **Code Execution Sandbox:** Implement the Podman driver using `bollard`.

## 📂 Key Files
- `crates/koad-plugins/Cargo.toml`
- `crates/koad-plugins/src/lib.rs`
- `crates/koad-plugins/wit/hooks.wit`
- `crates/koad-plugins/examples/hello-plugin/`

---
— Tyr, Captain of the Citadel
