# helm — Facts

*Stable, verified facts about the KoadOS environment.*

- KoadOS repo is at `/home/ideans/.koad-os` on Jupiter (WSL2/Ubuntu on Windows 11).
- Platform identity is defined in `/home/ideans/.koad-os/config/identities/helm.toml`.
- Vault is at `/home/ideans/.koad-os/agents/KAPVs/helm/`.
- Bay DB will be at `/home/ideans/.koad-os/bays/helm/state.db` (auto-provisioned by Citadel).
- Dood (Ian) is the final approval gate for all architectural changes.
